"""Integration tests for chatom.

These tests run against real backends when the appropriate
environment variables are set. They are skipped if credentials
are not available.

Run with: pytest chatom/tests/integration/ -v
"""
